---
description: 强制遵循 docs/方便人理解代码规约.md（中文 Javadoc、行数上限、功能块）
globs: "**/*.java"
alwaysApply: true
---

# Java 可读性注释（强制）

完整条文见 [`docs/方便人理解代码规约.md`](../../docs/方便人理解代码规约.md)。本规则为 **MANDATORY**，新代码与改动文件须在同次提交内满足。

## 必须做（§5.0）

- **标识符英文**；给人看的 **Javadoc、`/* 功能块 */`、行内 `//` 用简体中文**。
- **每个类**（含 package-private、测试类）：类型声明前 **中文 Javadoc**（职责；public 补充「不负责什么」）。
- **每个方法**（含 private、构造器、`@Test`）：方法声明前 **中文 Javadoc**；对外 public API 写前置条件、**幂等**、副作用。
- **单文件 ≤ 500 行**（含空行、注释、import）；**单方法 ≤ 50 行有效代码**（不计注释行与空行，见规约 §5.0 计法）。
- 每个模块/子包：`package-info.java`（或子模块 `pom.xml` description）。
- **复杂编排**（application 服务 public 方法、Gateway Filter/Processor、多步 `invoke`）：按 **§4.1** 用 `/* 功能块：… — … */` 分块，写 **why**。
- 改行为时同步改注释；禁止只合代码、注释「过后再补」。

## 禁止

- 用英文写给人看的类/方法 Javadoc（协议字段名等片段除外）。
- 行尾注释复述代码（`// 保存订单`）。
- 无类级/方法级中文 Javadoc 的新增或改动 API。
- 新增超过 500 行的文件，或把已超限文件继续加长；新增超过 50 行有效代码的方法（应抽取）。

## 提交前自检（规约 §13 精简）

1. **每个类、每个方法**是否都有中文 Javadoc？文件 ≤ 500 行？方法 ≤ 50 行有效代码？
2. 对外 public 方法是否说明幂等与前置条件？
3. 超长编排是否有 **§4.1** 功能块？
4. 新包是否补了 `package-info.java`？

未满足以上任一项，视为未完成该次改动。
