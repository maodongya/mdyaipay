<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mdyaipay.user.dao.merchant.mybatis.mapper.ShopMapper">

    <insert id="upsert">
        INSERT INTO shop (shop_id, merchant_id, shop_name, category, operating_status, created_at, updated_at)
        VALUES (#{shopId}, #{merchantId}, #{shopName}, #{category}, #{operatingStatus}, #{createdAt}, #{updatedAt})
        ON DUPLICATE KEY UPDATE
            operating_status = VALUES(operating_status),
            updated_at = VALUES(updated_at)
    </insert>

    <select id="findById" resultType="com.mdyaipay.user.dao.merchant.mybatis.row.ShopRow">
        SELECT shop_id, merchant_id, shop_name, category, operating_status, created_at, updated_at
        FROM shop
        WHERE shop_id = #{shopId}
    </select>
</mapper>
