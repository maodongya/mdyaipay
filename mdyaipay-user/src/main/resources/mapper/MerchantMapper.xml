<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mdyaipay.user.dao.merchant.mybatis.mapper.MerchantMapper">

    <insert id="upsert">
        INSERT INTO merchant (merchant_id, name, status, version, created_at, updated_at)
        VALUES (#{merchantId}, #{name}, #{status}, #{version}, #{createdAt}, #{updatedAt})
        ON DUPLICATE KEY UPDATE
            status = VALUES(status),
            version = version + 1,
            updated_at = VALUES(updated_at)
    </insert>

    <select id="findById" resultType="com.mdyaipay.user.dao.merchant.mybatis.row.MerchantRow">
        SELECT merchant_id, name, status, version, created_at, updated_at
        FROM merchant
        WHERE merchant_id = #{merchantId}
    </select>
</mapper>
