<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mdyaipay.payment.repository.mybatis.mapper.PaymentOrderMapper">

    <insert id="upsert">
        INSERT INTO payment_order (id, order_no, merchant_id, amount, channel, product_type, status, created_at, updated_at)
        VALUES (#{id}, #{orderNo}, #{merchantId}, #{amount}, #{channel}, #{productType}, #{status}, #{createdAt}, #{updatedAt})
        ON DUPLICATE KEY UPDATE status = VALUES(status), updated_at = VALUES(updated_at)
    </insert>

    <select id="findByOrderNo" resultType="com.mdyaipay.payment.repository.mybatis.row.PaymentOrderRow">
        SELECT id, order_no, merchant_id, amount, channel, product_type, status, created_at, updated_at
        FROM payment_order
        WHERE order_no = #{orderNo}
    </select>
</mapper>
