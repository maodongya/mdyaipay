<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN" monitorInterval="30">
    <Properties>
        <Property name="TIMETRACE_LOG">${spring:mdyaipay.timetrace.log-file:-logs/timetrace.log}</Property>
        <Property name="APP_LOG">${spring:logging.file.name:-logs/mdyaipay-payment.log}</Property>
        <Property name="LOG_PATTERN">%d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %-5level %logger{36} traceId=%X{traceId} spanId=%X{spanId} - %msg%n</Property>
    </Properties>

    <Appenders>
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="${LOG_PATTERN}"/>
        </Console>
        <RollingFile name="AppFile"
                     fileName="${APP_LOG}"
                     filePattern="${APP_LOG}.%d{yyyy-MM-dd}.gz">
            <PatternLayout pattern="${LOG_PATTERN}"/>
            <Policies>
                <TimeBasedTriggeringPolicy interval="1"/>
            </Policies>
            <DefaultRolloverStrategy max="14"/>
        </RollingFile>
        <RollingFile name="TimeTraceFile"
                     fileName="${TIMETRACE_LOG}"
                     filePattern="${TIMETRACE_LOG}.%d{yyyy-MM-dd}.gz"
                     immediateFlush="true">
            <PatternLayout pattern="%msg%n"/>
            <Policies>
                <TimeBasedTriggeringPolicy interval="1"/>
            </Policies>
            <DefaultRolloverStrategy max="30"/>
        </RollingFile>
    </Appenders>

    <Loggers>
        <Logger name="com.mdyaipay.tools.timetrace.report"
                level="info"
                additivity="false">
            <AppenderRef ref="TimeTraceFile"/>
        </Logger>
        <Root level="info">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="AppFile"/>
        </Root>
    </Loggers>
</Configuration>
