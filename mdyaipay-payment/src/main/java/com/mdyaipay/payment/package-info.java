/**
 * 支付核心模块（Spring Boot，默认端口 8081）。
 * <p>
 * 分层：{@code controller} / {@code service} / {@code domain} / {@code repository} / {@code mybatis}；
 * 渠道适配占位见 {@code gateway}（Mock 实现）。
 */
package com.mdyaipay.payment;
