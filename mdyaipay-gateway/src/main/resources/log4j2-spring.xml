<?xml version="1.0" encoding="UTF-8"?>
<!-- 统一 Log4j2：仅 Console + 单文件 RollingFile，勿对 logging.file.name 再 shell 重定向同一文件 -->
<Configuration status="WARN" monitorInterval="30">
    <Properties>
        <Property name="APP_LOG">${spring:logging.file.name:-logs/mdyaipay-gateway.log}</Property>
        <Property name="LOG_PATTERN">%d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %-5level %logger{36} traceId=%X{traceId} spanId=%X{spanId} spanLevelGlobal=%X{spanLevelGlobal} serverDepthLevel=%X{serverDepthLevel} - %msg%n</Property>
    </Properties>

    <Appenders>
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="${LOG_PATTERN}"/>
        </Console>
        <RollingFile name="AppFile"
                     fileName="${APP_LOG}"
                     filePattern="${APP_LOG}.%d{yyyy-MM-dd}.gz">
            <PatternLayout pattern="${LOG_PATTERN}"/>
            <Policies>
                <TimeBasedTriggeringPolicy interval="1"/>
            </Policies>
            <DefaultRolloverStrategy max="14"/>
        </RollingFile>
    </Appenders>

    <Loggers>
        <!-- 仅写 AppFile：Root 若同时挂 Console，且启动脚本把 stdout 重定向到同一日志文件，会出现逐行双份 -->
        <Root level="info">
            <AppenderRef ref="AppFile"/>
        </Root>
    </Loggers>
</Configuration>
